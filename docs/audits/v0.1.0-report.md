# v0.1.0 release audit

## Result

The implementation and documentation stages are complete through TASK-016. Exact
release-candidate verification results remain pending TASK-017. This document must
not be treated as release approval until every item in the Tests section records a
successful TASK-017 command and the tracker marks TASK-017 `DONE`.

### Baseline

- n-r-w/squirrel: v1.6.0 / `4c87dbba0f35938b7af0171bf00c4276238e7907`.
- Masterminds compatibility reference: v1.5.4 /
  `d8eb51bf129800f02602eaef1a44c220a69ccc36`.
- The baseline commit and upstream attribution are recorded in `UPSTREAM.md`.
- Deterministic exported API inventories are stored under `docs/audits/baseline/`.

### Core dependencies before

The n-r-w v1.6.0 root graph included pgx, scany, testdock/Docker infrastructure,
testify, `golang.org/x/exp`, and a large transitive module graph. The captured manifest
and full module list are:

- `docs/audits/baseline/n-r-w-v1.6.0-go.mod`;
- `docs/audits/baseline/n-r-w-v1.6.0-modules.txt`.

The fork graph immediately before root test cleanup is recorded in
`docs/audits/baseline/task-006-modules-before.txt`.

### Core dependencies after

The root module has one direct runtime dependency:

```text
github.com/muonsoft/squirrel
└── github.com/lann/builder
    └── github.com/lann/ps
```

The exact post-cleanup snapshot is
`docs/audits/baseline/task-006-modules-after.txt`. PostgreSQL and test assertion
dependencies are isolated in the nested `integration` module and are not part of a
root consumer's module graph.

### Preserved upstream features

- Masterminds-style Select, Insert, Update, Delete, expression, statement-builder,
  prefix/suffix, join, and placeholder APIs.
- Masterminds v1.5.4 `Case` semantics.
- n-r-w final-pass nested placeholder composition.
- CTE, recursive CTE, DML CTE, `UPDATE ... FROM`, nested subqueries, Exists/NotExists,
  scalar comparisons, Not, Coalesce, aggregate expressions, Range, and PostgreSQL
  `In`/`NotIn` behavior.
- Immutable-style builder reuse through `github.com/lann/builder`.

### Removed features

- Database execution, runner, scanning, row, and statement-cache APIs.
- Search and pagination policies.
- Conditional ordering and numeric sort-key mapping.
- `EqNotEmpty` zero-value omission policy.
- Stateful Select table-alias rewriting helpers.
- Root integration infrastructure dependencies and non-standard test assertions.

### Compatibility differences vs Masterminds v1.5.4

- The module import path is `github.com/muonsoft/squirrel`.
- Database execution and statement-cache APIs are intentionally absent.
- PostgreSQL-oriented builder primitives inherited from n-r-w are additional APIs.
- `In`/`NotIn` multi-item slices use a single PostgreSQL array argument with ANY/ALL.
- The complete exported-symbol decisions are documented in
  `docs/API_COMPATIBILITY.md`; the migration fixture covers typical import-only
  builder migrations.

### Regression bugs covered

- Continuous Dollar numbering across parent and child SELECTs.
- Nested builders in Eq, joins, `FromSelect`, columns, update values, And/Or trees,
  prefixes, suffixes, and custom Sqlizers.
- Escaped PostgreSQL question-mark operators without consumed arguments or numbering
  gaps.
- Continuous numbering through multiple, recursive, and DML CTEs.
- CTE and `UPDATE ... FROM` argument ordering.
- Immutable reuse of a base builder.
- Invalid SELECT, INSERT, CTE, expression, nested Sqlizer, and placeholder errors.
- Placeholder replacement and representative composition fuzz coverage.

### PostgreSQL integration scenarios

The integration suite contains executable coverage for:

- simple and nested SELECT;
- correlated EXISTS;
- CTE and recursive CTE;
- `UPDATE ... FROM` and DML CTE;
- `FOR UPDATE SKIP LOCKED`;
- INSERT, UPDATE, and DELETE with RETURNING;
- IN/NOT IN and ANY/ALL, including typed UUID slices;
- JSON question-mark operators;
- CASE and COALESCE.

TASK-017 must execute these tests against a real PostgreSQL service. A skipped suite
does not satisfy release validation.

### Tests

- format: **PENDING TASK-017** — `gofmt` cleanliness over the repository.
- vet: **PENDING TASK-017** — `go vet ./...`.
- unit: **PENDING TASK-017** — `go test ./...`.
- race: **PENDING TASK-017** — `go test -race ./...`.
- fuzz: **PENDING TASK-017** — focused placeholder fuzz smoke.
- compatibility: **PENDING TASK-017** — nested migration module.
- integration: **PENDING TASK-017** — real PostgreSQL execution, not skips.
- dependencies: **PENDING TASK-017** — policy, tidy, and module verification.
- Go versions: **PENDING TASK-017** — declared minimum and current supported toolchain.
- repository state: **PENDING TASK-017** — clean committed checkout and no v0.1.0 tag.

### Remaining risks

- `github.com/lann/builder` remains the immutable-builder implementation. Replacing it
  would be a separate compatibility-sensitive project.
- Arbitrary custom Sqlizers that return pre-numbered `$N` placeholders cannot be
  renumbered by a parent builder; custom nested fragments must return `?`.
- The PostgreSQL ANY/ALL behavior of `In`/`NotIn` is intentionally retained but may
  surprise callers expecting placeholder expansion.
- Identifiers and raw SQL text are trusted-input surfaces; placeholders protect
  values, not identifiers.
- Final Go 1.25/1.26 toolchain evidence and all current CI tooling behavior remain
  release-candidate validation work.

### Recommended follow-up work

- Run TASK-017 from a clean checkout and record exact commands, toolchain versions,
  PostgreSQL version, and results in this report.
- Review upstream changes periodically and port only focused, regression-tested fixes.
- Consider replacing `lann/builder` only after v0.1.0 stabilizes and only with a
  dedicated compatibility plan.
- If PostgreSQL-specific helpers expand materially, evaluate a separate
  `squirrel/postgres` package instead of broadening the root API.
